# Node / VitePress
node_modules/
docs/.vitepress/dist/
docs/.vitepress/cache/
dist/
.cache/

# npm / pnpm / yarn
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
package-lock.json
yarn.lock
pnpm-lock.yaml

# kaizen runtime state (generated per-project, not committed to the skill repo itself)
# NOTE: in *target* projects, .kaizen/ IS committed — this ignores it only in the skill repo
# Remove the next line in projects where you want to track .kaizen/ state
# .kaizen/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp
*.swo
*~
.project
.classpath

# Env / secrets
.env
.env.*
!.env.example
*.pem
*.key

# Playwright / test artifacts
test-results/
playwright-report/
blob-report/
playwright/.cache/
coverage/
.nyc_output/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/
dist/
build/

# Temporary files
*.tmp
*.bak
*.orig
board.html.tmp
tasks.json.tmp
